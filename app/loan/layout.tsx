export const metadata = {
  title: "Loan Calculator",
  description: "Estimate loan payments and browse car, boat, and RV calculators.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
